export type NavLink = {
  href: string;
  label: string;
};

export type Benefit = {
  title: string;
  description: string;
  kicker: string;
};

export type ProcessStage = {
  id: string;
  title: string;
  objective: string;
  actions: string[];
};
